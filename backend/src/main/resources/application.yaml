spring:
  application:
    name: backend
  jackson:
    property-naming-strategy: SNAKE_CASE
  web:
    error:
      whitelabel:
        enabled: false
  flyway:
    enabled: true
    locations:
      - classpath:db/migration